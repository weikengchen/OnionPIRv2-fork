[package]
name = "onionpir"
version = "0.1.0"
edition = "2021"
description = "Rust bindings for OnionPIRv2 — a high-performance Private Information Retrieval library"
links = "onionpir"
build = "build.rs"

[build-dependencies]
cmake = "0.1"
